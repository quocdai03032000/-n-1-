@model IEnumerable<DoAn1_QLTV_User_.Models.DauSach>

@{
    ViewBag.Title = "MainUser";
    Layout = "~/Areas/User/Views/Shared/_LayoutUser.cshtml";
}

@using (Html.BeginForm("MainUser", "User"))
{
    <div class="search_func">
        <div class="col-md-5">
            <div class="search">
                <input type="text" placeholder="Tìm kiếm" name="text">
                <button type="submit"><i class="fa fa-search"></i></button>
            </div>
        </div>
    </div>
}

<div class="container" style="margin-top:10px;">
    <div class="row">
        @foreach (var item in Model)
        {
        <div class="col-sm-12 col-lg-3">
@*            <div class="card-pro">
                <a href="@Url.Action("Details_Book","User",new { id=item.MaDauSach })">
                    <img src="@Url.Content(item.HinhAnh)" style="height:150px;width:150px" />
                </a>
                <p>@Html.DisplayFor(model => item.TenSach)</p>
                <p>@Html.DisplayFor(model => item.TacGia)</p>
                <p>@Html.DisplayFor(model => item.TheLoai)</p>
            </div>*@
            <div class="card" style="width: 220px; height:350px; margin-top:10px;">
                <a href="@Url.Action("Details_Book","User",new { id=item.MaDauSach })">
                    <img class="card-img-top" src="@Url.Content(item.HinhAnh)" alt="Card image cap" style="height:220px; width:200px;">
                </a>
                <div class="card-body">
                    <h5 class="card-title">@Html.DisplayFor(model => item.TenSach)</h5>
                    <p>@Html.DisplayFor(model=>item.TacGia)</p>
                    <a href="@Url.Action("Details_Book","User",new {id=item.MaDauSach })" class="btn btn-primary" style="color:red;">Xem chi tiết</a>
                </div>
            </div>
        </div>
        }
    </div>
</div>
